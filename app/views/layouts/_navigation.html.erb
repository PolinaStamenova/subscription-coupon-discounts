<!-- Sidebar -->
<aside class="h-screen w-54 bg-black text-white flex flex-col">
  <!-- Logo -->
  <div class="h-16 flex items-center px-4 border-b border-gray-700 rounded-b-lg">
    <%= link_to root_path, class: "text-xl " do %>
      Subscription system
    <% end %>
  </div>

  <!-- Menu Items -->
  <nav class="flex-1 px-4 py-6">
    <ul class="space-y-2 font-medium">
      <li>
        <%= link_to "Plans", plans_path, class: "block hover:bg-purple-200 hover:text-black px-3 py-2 rounded-xl text-sm #{current_page?(plans_path) ? 'border border-white' : ''}" %>
      </li>
      <li>
        <%= link_to "Coupons", coupons_path, class: "block hover:bg-purple-200 hover:text-black px-3 py-2 rounded-xl text-sm #{current_page?(coupons_path) ? 'border border-white' : ''}" %>
      </li>
    </ul>
  </nav>
</aside>
